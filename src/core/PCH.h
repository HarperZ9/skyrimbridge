#pragma once

// Include SKSE PCH first - this sets up the stl namespace alias for REL
#include <SKSE/Impl/PCH.h>

// RE/Skyrim.h includes everything we need
#include <RE/Skyrim.h>
#include <SKSE/SKSE.h>

// Required for string literals (sv suffix) in global namespace
using namespace std::literals;
